#include <stdio.h>
#include <stdlib.h>

int main (void) {

    int * array = (int*) malloc(sizeof(int) * 9);
    for (int i=0; i<9; i++)
        array[i] = i+1;
    printf("array: ");
    for (int i=0; i<9; i++)
        printf(("%d ", array[i]);
    printf("\n");
    int ** array2d;
    for (int i=0; i<9; i++) {
        int row = i/3;
        int col = i%3;
        &array2d[row][col] = &array[i];
    } 
    for (int i=0; i<3; i++)
        for (int j=0; j<3; j++)
          
} 
